<!DOCTYPE html>
<html lang="en">
  <head>
    <meta charset="UTF-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <meta http-equiv="X-UA-Compatible" content="ie=edge" />
    <link rel="stylesheet" href="../css/reset.css" />
    <link rel="stylesheet" href="../css/root.css" />
    <link rel="stylesheet" href="../css/main.css" />
    <link
      rel="stylesheet"
      href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/5.12.1/css/all.min.css"
    />
    <title>KonfinityPost</title>
  </head>
  <body>
    <nav>
      <div id="nav-logo">
        <p class="logo"><i class="fas fa-envelope-open"></i>KonfinityPost</p>
      </div>
      <div id="nav-menu">
        <a href="/" class="menu-button">Campaigns</a>
        <a href="/lists" class="menu-button">Lists</a>
        <a href="" class="menu-button">Reports</a>
      </div>
    </nav>

    <section id="main-section">
      <div id="sidebar">
        <button
          class="panel-button-large"
          onclick="window.location.href = '/campaign'"
        >
          Create a Campaign
        </button>
        <button class="panel-button">Send a Campaign</button>
        <a href="" class="current"><i class="fas fa-envelope"></i>Campaigns</a>
        <a href=""><i class="fas fa-check-circle"></i>Completed Queue</a>
      </div>
      <div id="main-content">
        <h1><i class="fas fa-envelope"></i>Create an Email Campaign</h1>
        <p class="subject">Email Campaign Details</p>
        <form
          action="/campaigns/edit?id=<%= campaign[0]._id %>"
          method="POST"
          enctype="multipart/form-data"
        >
          <div>
            <label for="name">Email Campaign Name</label>
            <input type="text" name="name" value="<%= campaign[0].name %>" />
          </div>

          <div>
            <label for="format">Email Campaign Format</label>
            <select name="format" id="format" value="<%= campaign[0].format %>">
              <option value="html">HTML (Recommended)</option>
              <option value="markdown">Markdown</option>
            </select>
          </div>

          <div>
            <label for="subject">Email Subject</label>
            <input
              type="text"
              name="subject"
              value="<%= campaign[0].subject %>"
            />
          </div>

          <div>
            <label for="file">Upload Content</label>
            <input
              type="file"
              name="file"
              value="<%= campaign[0].file_url %>"
            />
          </div>

          <div>
            <button class="action-button">Submit</button>
          </div>
        </form>
      </div>
    </section>
  </body>
</html>
